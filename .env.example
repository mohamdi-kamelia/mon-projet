# Frontend (baked into JS bundle at build time)
VITE_BACKEND_URL=https://api.yourdomain.com

# Frontend host port (default: 80, change if port 80 is in use)
# FRONTEND_PORT=80

# BigBlueButton
BBB_URL=https://bbb.yourdomain.com/bigbluebutton
BBB_SECRET=your-bbb-shared-secret
